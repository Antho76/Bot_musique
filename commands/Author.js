const discord = require("discord.js");

module.exports.run = async (client, message, args ) => {
message.channel.send (" BOT cxréé par Anthonin PAIN ");
}

module.exports.help = {
    name: "Author"
}